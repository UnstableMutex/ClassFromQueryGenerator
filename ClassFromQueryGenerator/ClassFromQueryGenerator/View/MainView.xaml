<UserControl x:Class="ClassFromQueryGenerator.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cap="clr-namespace:CAP;assembly=CAP"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <cap:CoupleAlignmentPanel DockPanel.Dock="Top">
          
              <Label>CS:</Label>
            <TextBox Text="{Binding CS}"/>
            <Label>Query:</Label>
            <TextBox Text="{Binding Query}"/>        
            
            <Label>Macros:</Label>
            <ComboBox />
        </cap:CoupleAlignmentPanel> 
        <Button DockPanel.Dock="Bottom" Command="{Binding GenerateCommand}" >Do</Button>
        <TextBox DockPanel.Dock="Top" Text="{Binding Result}"/>
      
    </DockPanel>
</UserControl>
